import 'package:flutter/material.dart';

class RecoverPassowordPage extends StatelessWidget {

  const RecoverPassowordPage({ super.key });

   @override
   Widget build(BuildContext context) {
       return Scaffold(
           appBar: AppBar(title: const Text('Recuperar Senha'),),
           body: Container(),
       );
  }
}